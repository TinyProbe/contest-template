#include <bits/stdc++.h>
#define int int64_t
#define endl '\n'
using namespace std;

void solve() {
}

int32_t main() {
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  int t = 0;
  if (t == 0) { cin >> t; }
  while (t--) { solve(); }
}
