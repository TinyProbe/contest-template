#include <bits/stdc++.h>
using namespace std;
#define int int64_t
#define endl '\n'
#define Int (cin >> g_i, g_i)
#define Str (cin >> g_s, g_s)
int g_i; string g_s;

void solve() {
}

int32_t main() {
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  for (int i = (0 ? 1 : Int); i; --i) { solve(); }
}
